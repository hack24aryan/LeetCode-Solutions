        memset(seen,0,sizeof(seen));
    }
        for(i=left;i<=right;i++)
        {
        }
        right++;
        left++;
    for(i=0;i<51;i++)
    {
        if(freq[i]==1)
    }

    int small=-1;
            small=i;
        return small;
    {
    while(right!=numsSize)
    int seen[51]={0};

            if(seen[nums[i]]==0){
    int i,freq[51]={0},left=0,right=k-1,value=1;
                freq[nums[i]]++;
                seen[nums[i]]=1;
                }
}
