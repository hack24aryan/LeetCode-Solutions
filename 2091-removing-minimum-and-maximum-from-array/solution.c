            indexmax=i;}
            indexmin=i;
            max=nums[i];
}        if(max<nums[i]){

    }
    int smallindex,largeindex;


    smallindex=indexmin<indexmax?indexmin:indexmax;
    largeindex=indexmin>indexmax?indexmin:indexmax;
    int fromfront=largeindex+1;

    int fromback=numsSize-smallindex;
    int both=(smallindex+1)+(numsSize-largeindex);
    int answer = fromfront;

            min=nums[i];
        if(min>nums[i]){
    {
    for(i=0;i<numsSize;i++)
    int i,indexmin=0,indexmax=0;

int minimumDeletions(int* nums, int numsSize) {
    int min=nums[0],max=nums[0];
